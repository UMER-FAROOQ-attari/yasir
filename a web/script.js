// If you want to dynamically change the cards or add more functionality, you can do it here.
document.addEventListener("DOMContentLoaded", () => {
  console.log("Cards are ready!");
  // You can dynamically fetch and add cards here if needed
});
